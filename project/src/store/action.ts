import {createAction} from '@reduxjs/toolkit';
import {TProduct, TPromo, TReview} from '../types/types';

//export const loadProduct = createAction<TProduct|null>('data/loadProduct');
//export const loadSimilarProducts = createAction<TProduct[]>('data/loadSimilarProducts');
//export const loadReviews = createAction<TReview[]>('data/loadReviews');
//export const loadPromo = createAction<TPromo>('data/loadPromo');

/*export const addItemToBasket = createAction('data/addItemToBasket');
export const setCandidateForBasket = createAction('data/setCandidateForBasket',
  (value: number) => ({
    payload: value,
  })
);*/

//export const setDataLoadedStatus = createAction<boolean>('data/setDataLoadedStatus');
//export const setReviewsDataLoadingStatus = createAction<boolean>('data/setReviewsDataLoadingStatus');


